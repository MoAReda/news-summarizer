## Prerequisites
Python 3.9 or later.
## Install Dependencies
Install the required Python libraries using pip:
Run this command 
pip install langchain langchain-groq sentence-transformers chromadb requests

## Run main.py and follow the instructions of the program 